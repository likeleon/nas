block vars
doctype html
html
  head
    block title
      title=title

    != env.getManifestFiles("static")
    meta(name="viewport", content="width=device-width, initial-scale=1.0")

  body
    block content
