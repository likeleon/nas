script(id="partials/main.html", type="text/ng-template")

div.container(ng-controller="FilesCtrl")
  div.table-responsive
    table#files.table.table-hover
      thead
        tr
          th Name
          th Size
          th Modified
      tbody
        tr(ng-repeat="node in fileNodes()", ng-switch="node.type", ng-click="nodeClicked(node)")
          td
            i.fa.fa-folder(ng-switch-when="directory")
            i.fa.fa-file(ng-switch-when="file")
            span {{node.name}}
          td(ng-switch-when="directory")
          td(ng-switch-when="file") {{(node.size / 1024) | number:2}} KB
          td {{node.modifiedTime | date:'medium'}}